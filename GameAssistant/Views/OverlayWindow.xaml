<Window x:Class="GameAssistant.Views.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="游戏建议" Height="400" Width="350"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">
    <Border Background="#CC000000" CornerRadius="10" BorderBrush="#FF888888" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- 标题栏 -->
            <Border Grid.Row="0" Background="#AA000000" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="游戏建议" Foreground="White" 
                               FontSize="14" FontWeight="Bold" Margin="10,5"/>
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button x:Name="MinimizeButton" Content="—" Width="30" Height="25" 
                                Background="Transparent" Foreground="White" BorderThickness="0"
                                Click="MinimizeButton_Click"/>
                        <Button x:Name="CloseButton" Content="×" Width="30" Height="25" 
                                Background="Transparent" Foreground="White" BorderThickness="0"
                                Click="CloseButton_Click"/>
                    </StackPanel>
                </Grid>
            </Border>
            
            <!-- 建议列表 -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="5">
                <ItemsControl x:Name="AdviceItemsControl">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#AA333333" CornerRadius="5" Margin="0,2" Padding="8">
                                <StackPanel>
                                    <TextBlock Text="{Binding Content}" Foreground="White" 
                                               TextWrapping="Wrap" FontSize="12"/>
                                    <TextBlock Text="{Binding Scenario}" Foreground="#FFAAAAAA" 
                                               FontSize="10" Margin="0,4,0,0"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
            
            <!-- 状态栏 -->
            <Border Grid.Row="2" Background="#AA000000" Padding="5">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock x:Name="StatusText" Text="等待识别..." Foreground="#FFAAAAAA" 
                               FontSize="10" Grid.Column="0"/>
                    <TextBlock x:Name="FPSText" Text="FPS: 0" Foreground="#FFAAAAAA" 
                               FontSize="10" Grid.Column="1" Margin="10,0,0,0"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
