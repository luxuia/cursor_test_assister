# 模板资源获取指南

## 🌐 在线资源获取方法

### 方法一：使用浏览器扩展工具

#### Chrome扩展推荐

1. **Image Downloader**
   - 可以批量下载网页上的图片
   - 支持筛选和预览

2. **Fatkun Batch Download Image**
   - 强大的图片批量下载工具
   - 支持按尺寸筛选

#### 使用步骤

1. 安装扩展
2. 打开游戏官网或资源网站
3. 点击扩展图标
4. 选择要下载的图片
5. 批量下载

### 方法二：使用Python脚本下载（高级）

创建一个简单的下载脚本：

```python
import requests
from bs4 import BeautifulSoup
import os

def download_images(url, save_dir):
    """从网页下载图片"""
    response = requests.get(url)
    soup = BeautifulSoup(response.content, 'html.parser')
    
    # 查找所有图片标签
    images = soup.find_all('img')
    
    for img in images:
        img_url = img.get('src')
        if img_url:
            # 下载图片
            img_data = requests.get(img_url).content
            filename = os.path.basename(img_url)
            with open(os.path.join(save_dir, filename), 'wb') as f:
                f.write(img_data)
            print(f"下载: {filename}")
```

### 方法三：使用游戏数据提取工具

#### 工具推荐

1. **AssetStudio**（Unity游戏）
   - 可以提取Unity游戏的资源
   - 支持导出图片和模型

2. **QuickBMS**（通用）
   - 支持多种游戏格式
   - 可以提取游戏资源文件

#### 使用注意事项

⚠️ **法律风险**：
- 仅用于个人学习和研究
- 不要用于商业用途
- 尊重游戏开发者的版权

## 🎮 常见游戏资源位置

### 英雄联盟 (League of Legends)

- **官方资源**：Riot Games API
- **社区资源**：社区制作的资源包
- **数据文件**：游戏安装目录的assets文件夹

### DOTA 2

- **Steam Workshop**：社区制作的资源
- **游戏文件**：Steam安装目录的资源文件

### 其他MOBA游戏

- 查看游戏官网的资源页面
- 社区论坛的资源分享区
- GitHub上的开源资源项目

## 🔍 搜索技巧

### Google搜索关键词

```
游戏名称 + "icon" + "png"
游戏名称 + "sprite" + "download"
游戏名称 + "assets" + "extract"
```

### GitHub搜索

```
游戏名称 + assets
游戏名称 + icons
游戏名称 + resources
```

### 社区资源

- Reddit游戏社区
- Discord游戏频道
- 游戏官方论坛

## 📥 下载后的处理

### 1. 图片预处理

使用Python脚本批量处理：

```python
from PIL import Image
import os

def process_images(input_dir, output_dir):
    """批量处理图片"""
    for filename in os.listdir(input_dir):
        if filename.endswith(('.png', '.jpg', '.jpeg')):
            img = Image.open(os.path.join(input_dir, filename))
            
            # 调整大小（如果需要）
            img = img.resize((64, 64), Image.Resampling.LANCZOS)
            
            # 转换为PNG（如果需要）
            if filename.endswith(('.jpg', '.jpeg')):
                filename = filename.rsplit('.', 1)[0] + '.png'
            
            # 保存
            img.save(os.path.join(output_dir, filename), 'PNG')
            print(f"处理: {filename}")
```

### 2. 批量重命名

```python
import os

def rename_files(directory, prefix):
    """批量重命名文件"""
    files = sorted(os.listdir(directory))
    for i, filename in enumerate(files, 1):
        ext = os.path.splitext(filename)[1]
        new_name = f"{prefix}_{i:03d}{ext}"
        os.rename(
            os.path.join(directory, filename),
            os.path.join(directory, new_name)
        )
        print(f"重命名: {filename} -> {new_name}")
```

## ⚠️ 重要提醒

### 版权和法律

1. **尊重版权**
   - 游戏资源通常受版权保护
   - 仅用于个人学习和研究
   - 不要用于商业用途

2. **使用条款**
   - 遵守游戏的使用条款
   - 不要违反服务协议

3. **社区准则**
   - 尊重社区规则
   - 不要滥用资源

### 最佳实践

1. **优先使用官方资源**
   - 游戏官网提供的资源
   - 官方API提供的接口

2. **社区资源**
   - 使用社区分享的开源资源
   - 遵守开源协议

3. **自己制作**
   - 从游戏截图提取
   - 使用模板采集工具

## 🛠️ 实用工具推荐

### 图片处理

- **GIMP**：免费图片编辑器
- **Paint.NET**：Windows图片编辑器
- **ImageMagick**：命令行图片处理工具

### 批量处理

- **Bulk Rename Utility**：批量重命名工具
- **Image Resizer**：批量调整图片大小
- **XnConvert**：批量图片转换工具

### 资源提取

- **AssetStudio**：Unity资源提取
- **QuickBMS**：通用资源提取工具
- **HxD**：十六进制编辑器

## 📝 模板管理建议

### 目录结构

```
Templates/
├── Heroes/
│   ├── hero_001.png
│   ├── hero_002.png
│   └── ...
├── Equipment/
│   ├── weapon_001.png
│   └── ...
├── Skills/
│   └── ...
└── UI/
    └── ...
```

### 命名规范

- 使用有意义的名称
- 统一命名格式
- 添加版本号（如需要）

### 版本管理

- 使用Git管理模板
- 添加变更日志
- 备份重要模板

## 🎯 快速开始

1. **确定目标游戏**
2. **查找资源位置**
3. **下载或提取资源**
4. **预处理图片**
5. **使用模板采集工具导入**
6. **测试识别效果**

完成！现在你可以开始获取模板资源了。
